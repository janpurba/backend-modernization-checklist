# Sample Service

The service boundary owns sample records. Its API consumers are documented in OpenAPI.

The critical-journey SLO is measured through request success and latency. Deployments use a canary stage with an explicit rollback threshold.
