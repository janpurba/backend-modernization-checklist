name: ci
on: [push]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./mvnw verify
      - uses: gitleaks/gitleaks-action@v2
      - run: trivy fs .
